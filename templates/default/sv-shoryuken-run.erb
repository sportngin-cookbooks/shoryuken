#!/bin/sh

set -e

PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
<% if @options[:app_dir] %>

APP_ROOT=<%= @options[:app_dir] %>
cd $APP_ROOT

<% end %>

exec 2>&1

exec <%= @options[:command] %>
